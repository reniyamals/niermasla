version: 2
jobs:
  abas:
    docker:
      - image: python:3.8

    steps:
    - checkout
    - run: sleep 2s && ls
    - run: sleep 2s && ls
    - run: sleep 2s && ls
    - run: ls && ls
    - run: ls && ls


workflows:
  version: 2
  abas:
    jobs:
    - abas
